spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_SERVICE_MAIL_USERNAME:ngoquanghuy0310@gmail.com}
    password: ${EMAIL_SERVICE_MAIL_PASSWORD:qmpiiudifabgpyop}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  application:
    name: email-service
  data:
    redis:
      host: localhost
      port: 6379
    mongodb:
      uri: mongodb+srv://${MONGO_DB_USERNAME:admin}:${MONGO_DB_PASSWORD:admin123}@restora.xde1bim.mongodb.net/email_service?retryWrites=true&w=majority
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 10
      delivery-timeout-ms: 120000
      enable-idempotence: true
      linger-ms: 10
      batch-size: 32768
      transaction-id-prefix: auth-service-tx-
      properties:
        max.in.flight.requests.per.connection: 5 // batch.size / linger.ms
        compression.type: lz4
    template:
      default-topic: user-registered.v1
server:
  port: 8702
# Google reCAPTCHA
google:
  recaptcha:
    secret-key: ${CAPTCHA_SECRET_KEY}
    site-key: ${CAPTCHA_SITE_KEY}
